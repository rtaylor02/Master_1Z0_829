# LVTI (Local Variable Type Inferencing)
var is not a type but ...

Rules of using var:
* Local variables only
* No multiple declaration
* Cannot be used for fields, method's return type, method's parameter