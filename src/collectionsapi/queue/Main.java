package collectionsapi.queue;

/**
 * Queue:
 * - insertion order from head (of the queue)
 * - process 1 element at head by FIFO or LIFO
 * - fixed size
 * - allow duplicates
 *
 * Commonly used methods:
 * - adding elements  : add(), offer(), put()
 * - removing elements: remove(), poll(), take(), drainTo()
 * - querying         : element(), peek()
 */
public class Main {}
